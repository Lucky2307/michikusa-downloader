# michikusa-downloader
```
usage: michikusa-dl.py [-h] [-o OUTPUT] [-t TIMEOUT] URL

Use reader link

positional arguments:
  URL                   Input URL

optional arguments:
  -h, --help            show this help message and exit
  -o OUTPUT, --Output OUTPUT
                        Output dir, default is out/
  -t TIMEOUT, --Timeout TIMEOUT
                        Timeout in seconds, default is 20
```

TODO:
1. Progress report